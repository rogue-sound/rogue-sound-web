import styled from 'styled-components';

const DeviceSelectorItemWrapper = styled.div`
  padding: 10px;
  font-size: 12px;
  font-weight: 500;
  cursor: pointer;
  &:hover {
    background-color: rgba(0, 0, 0, 0.1);
  }
`;

export { DeviceSelectorItemWrapper };
